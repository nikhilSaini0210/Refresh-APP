import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ProfileVisit = () => {
  return (
    <View>
      <Text>ProfileVisit</Text>
    </View>
  );
};

export default ProfileVisit;

const styles = StyleSheet.create({});
